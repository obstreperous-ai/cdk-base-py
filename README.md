# cdk-empty-py
Template for CDK Python repositories

## Dev Container
This repository includes a dev container configuration for Codespaces/local devcontainer use.

Included tooling:
- Python 3.12
- Node.js (for AWS CDK CLI)
- Common command-line utilities
- AWS CDK CLI (`cdk`)
